{
  "name": "byte-sized-business-boost",
  "version": "1.0.0",
  "description": "Local business discovery app",
  "main": "index.js",
  "scripts": {
    "start": "netlify dev",
    "migrate": "node scripts/migrate.js",
    "build": "echo 'Build complete'",
    "prod-build": "npm run migrate && echo 'Build complete'"
  },
  "dependencies": {
    "pg": "^8.11.0"
  }
}
